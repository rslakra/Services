name: Samples WebApp - LogWebApp CI

on:
  push:
    paths:
      - "Samples/Python/LogWebApp/**"
      - ".github/workflows/samples-log-webapp-ci.yml"
  pull_request:
    paths:
      - "Samples/Python/LogWebApp/**"
      - ".github/workflows/samples-log-webapp-ci.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: Samples/Python/LogWebApp

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: Samples/Python/LogWebApp/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run unit tests
        run: python -m unittest discover -s tests -p "test_*.py" -t . -v
