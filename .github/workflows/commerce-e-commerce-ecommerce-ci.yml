name: Commerce - e-commerce CI

on:
  push:
    paths:
      - "Commerce/e-commerce/e-commerce/**"
      - ".github/workflows/e-commerce-ci.yml"
  pull_request:
    paths:
      - "Commerce/e-commerce/e-commerce/**"
      - ".github/workflows/e-commerce-ci.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: Commerce/e-commerce/e-commerce

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: maven
          cache-dependency-path: Commerce/e-commerce/e-commerce/pom.xml

      - name: Build and run unit tests
        run: mvn -B -ntp test
